diff --git a/build/lib/util.ts b/build/lib/util.ts
index 0000000..1111111 100644
--- a/build/lib/util.ts
+++ b/build/lib/util.ts
@@ -9,7 +9,6 @@ import _debounce from 'debounce';
 import _filter from 'gulp-filter';
 import rename from 'gulp-rename';
 import path from 'path';
 import fs from 'fs';
-import * as rimrafModule from 'rimraf';
 import VinylFile from 'vinyl';
 import { ThroughStream } from 'through';
 import sm from 'source-map';
@@ -17,6 +16,9 @@ import { pathToFileURL } from 'url';
 import ternaryStream from 'ternary-stream';

 const root = path.dirname(path.dirname(__dirname));
+
+// Use require for rimraf 2.2.8 (CommonJS module, no default export)
+const rimrafModule = require('rimraf');

 export function cleanBuildTask(outDir: string, excludes: string[]): () => Promise<void> {
 	const result = () => new Promise<void>((c, e) => {
 		const patterns = excludes.map(exclude => `!${outDir}/${exclude}`);
